#include <stdio.h>
/**
 * main - Entry
 *
 * Return: 0
 */
int main(void)
{
	char k;

	for (k = 48; k <= 57; k++)
	{
		putchar(k);
	}
	for (k = 97; k <= 102; k++)
	{
		putchar(k);
	}
	putchar('\n');
	return (0);
}
